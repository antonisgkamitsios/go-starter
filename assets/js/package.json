{
  "name": "integration-react",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "esbuild --bundle index.tsx --outdir=../static --sourcemap --define:process.env.NODE_ENV='\"development\"'  --global-name=bundle --watch",
    "build": "esbuild --bundle index.tsx --outdir=../static --minify  --global-name=bundle"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react-dom": "^18.2.18",
    "esbuild": "^0.24.0",
    "typescript": "^5.3.3"
  }
}
